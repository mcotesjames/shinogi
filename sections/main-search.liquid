{{ 'template-collection.css' | asset_url | stylesheet_tag }}
{{ 'component-article-card.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-search.css' | asset_url | stylesheet_tag }}
{{ 'template-search.css' | asset_url | stylesheet_tag }}

{%- if section.settings.predictive_search_enabled -%}
  {{ 'component-predictive-search.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'predictive-search.js' | asset_url }}" defer="defer"></script>
{%- endif -%}

{%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
  {{ 'component-facets.css' | asset_url | stylesheet_tag }}
  <script src="{{ 'facets.js' | asset_url }}" defer="defer"></script>
{%- endif -%}
{% if section.settings.enable_card_size_toggle %}
  <script src="{{ 'catalog-toggle-card-size.js' | asset_url }}" defer="defer"></script>
{% endif %}

{%- style -%}
  {% render 'section-margin' %}

  predictive-search .spinner {
    width: 1.5rem;
    height: 1.5rem;
  }
{%- endstyle -%}

{%- liquid
  assign sort_by = search.sort_by | default: search.default_sort_by
  assign terms = search.terms | escape
  assign search_url = '?q=' | append: terms | append: '&options%5Bprefix%5D=last&sort_by=' | append: sort_by
-%}

<div class="section-{{ section.id }}-padding template-search {% if search.performed == true and search.results_count == 0 and terms != blank and section.settings.enable_filtering == false and section.settings.enable_sorting == false %} template-search--empty {% endif %}">
  <div class="template-search__header">
    <div class="search-container {% if search.performed and search.results_count > 0 %}search-container--performed{% elsif search.performed == true and search.results_count == 0 %}search-container--empty{% else %}search-container--empty{% endif %}">
      <div class="page-header">
        <div class="page-header__inner">
          {%- if search.performed and search.results_count == 0 -%}
            <h1 class="page-header__inner-title h6" role="status">
              {{ 'templates.search.no_results' | t: terms: search.terms }}
            </h1>
            <p class="page-header__inner-text body-small">
              {{ 'templates.search.no_results_text' | t }}
            </p>
          {%- elsif search.performed and search.results_count == 1 -%}
            <h1 class="page-header__inner-title h6">
              {{ 'templates.search.title.one' | t: count: search.results_count }}: "{{ terms }}"
            </h1>
          {%- elsif search.performed and search.results_count > 1 -%}
            <h1 class="page-header__inner-title h6">
              {{ 'templates.search.title.many' | t: count: search.results_count }}: "{{ terms }}"
            </h1>
          {%- else -%}
            <h1 class="page-header__inner-title h6">{{- 'general.search.search' | t -}}</h1>
          {%- endif -%}
        </div>
      </div>

      <div class="template-search__search">
        {%- if section.settings.predictive_search_enabled -%}
          <predictive-search data-loading-text="{{ 'accessibility.loading' | t }}">
        {%- endif -%}
        {%- if search.performed and search.results_count == 0 and search.filters == empty -%}
          <div class="search-wrapper">
        {% endif %}
        <form
          action="{{ routes.search_url }}"
          method="get"
          role="search"
          class="search{% if search.performed %} search--perfomed{% endif %}"
        >
          <div class="search__form-inner">
            <div class="field">
              <input
                class="search__input field__input"
                id="Search-In-Template"
                dir="ltr"
                type="search"
                name="q"
                value="{{ search.terms | escape }}"
                placeholder="{{ 'general.search.placeholder' | t }}"
                autofocus
                {%- if section.settings.predictive_search_enabled -%}
                  role="combobox"
                  aria-expanded="false"
                  aria-owns="predictive-search-results-list"
                  aria-controls="predictive-search-results-list"
                  aria-haspopup="listbox"
                  aria-autocomplete="list"
                  autocorrect="off"
                  autocomplete="off"
                  autocapitalize="off"
                  spellcheck="false"
                {%- endif -%}
              >
              <label for="Search-In-Template">{{ 'general.search.placeholder' | t }}</label>
              <input name="options[prefix]" type="hidden" value="last">

              <button
                class="search__button search__button--reset field__button focus-inset"
                aria-label="{{- 'general.search.reset_search' | t -}}"
                type="reset"
                {%- if search.terms != blank -%}
                  style="display: flex;"
                {%- endif -%}
              >
                {% render 'icon-close' %}
              </button>
              <button
                class="search__button field__button focus-inset"
                aria-label="{{- 'general.search.search' | t -}}"
                type="submit"
              >
                {% render 'icon-search-input' %}
              </button>
            </div>
          </div>
          {%- if section.settings.predictive_search_enabled -%}
            <div class="predictive-search predictive-search--search-template" tabindex="-1" data-predictive-search>
              <div class="predictive-search__loading-state">
                {% render 'icon-loading' %}
              </div>
            </div>
            <span class="predictive-search-status visually-hidden" role="status" aria-hidden="true"></span>
          {%- endif -%}
        </form>

        {%- if search.performed and search.results_count == 0 and search.filters == empty -%}
          </div>
        {% endif %}
        {%- if section.settings.predictive_search_enabled -%}
          </predictive-search>
        {%- endif -%}
        {%- if search.performed == false
          and section.settings.enable_trending_search
          and section.settings.trending_search_menu != blank
        -%}
          <div class="trending-search">
            {% if section.settings.trending_search_title != blank -%}
              <h3 class="trending-search-title subtitle">
                {{- section.settings.trending_search_title -}}
              </h3>

              {%- if section.settings.trending_search_menu != blank -%}
                {% assign search_menu = section.settings.trending_search_menu %}
                <div class="predictive-search-results-wrapper">
                  <div
                    id="trending-search-list"
                    class="predictive-search__results-list list-unstyled"
                    role="group"
                    tabindex="-1"
                  >
                    <div class="header__search-popular-links first-column">
                      {%- for link in search_menu.links limit: 4 -%}
                        <div class="header__search-popular-link-wrapper ">
                          <a
                            href="{{ link.url }}"
                            class="header__search-popular-link hover-underlined-link-secondary body-small focus-inset{% if forloop.index < 5 %} first-column{% else %} second-column{% endif %}"
                            role="option"
                          >
                            <span>{{- link.title | escape -}}</span>
                          </a>
                        </div>
                      {%- endfor -%}
                    </div>

                    {% if search_menu.size > 4 %}
                      <div class="header__search-popular-links second-column">
                        {%- for link in search_menu.links offset: 4 limit: 4 -%}
                          <div class="header__search-popular-link-wrapper ">
                            <a
                              href="{{ link.url }}"
                              class="header__search-popular-link hover-underlined-link-secondary body-small focus-inset{% if forloop.index < 5 %} first-column{% else %} second-column{% endif %}"
                              role="option"
                            >
                              <span>{{- link.title | escape -}}</span>
                            </a>
                          </div>
                        {%- endfor -%}
                      </div>
                    {% endif %}
                  </div>
                </div>
              {%- endif -%}
            {% endif %}
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>

  {%- if search.performed -%}
    {% liquid
      assign filter_show = false
      for filter in search.filters
        if filter.range_max != 0
          assign filter_show = true
        endif
      endfor
    %}

    <div>
      {% comment %} ACTIVE DESKTOP FILTERS {% endcomment %}
      {%- if section.settings.enable_filtering -%}
        <div class="facets__top facets ">
          <div class="active-facets active-facets-desktop">
            {% assign active_facets = false %}
            {%- for filter in search.filters -%}
              {% if filter.active_values.size > 0 or filter.min_value.value != null or filter.max_value.value != null %}
                {% assign active_facets = true %}
                {% break %}
              {% endif %}
            {% endfor %}

            <div class="active-facets__wrapper {% if active_facets %} active-facets__wrapper_margin {% endif %}">
              {%- for filter in search.filters -%}
                {%- for value in filter.active_values -%}
                  <facet-remove>
                    <a href="{{ value.url_to_remove }}" class="active-facets__button focus-inset">
                      <span class="active-facets__button-inner body-small">
                        <span>{{ value.label | escape }}</span>
                        {% render 'icon-close' %}
                      </span>
                      <span class="visually-hidden">{{ 'products.facets.clear_filter' | t }}</span>
                    </a>
                  </facet-remove>
                {%- endfor -%}
                {% if filter.type == 'price_range' %}
                  {%- if filter.min_value.value != null or filter.max_value.value != null -%}
                    <facet-remove>
                      <a href="{{ filter.url_to_remove }}" class="active-facets__button focus-inset">
                        <span class="active-facets__button-inner body-small">
                          <span>
                            {%- if filter.min_value.value -%}
                              {{ filter.min_value.value | money }}
                            {%- else -%}
                              {{ 0 | money }}
                            {%- endif -%}
                            —
                            {% if filter.max_value.value %}
                              {{ filter.max_value.value | money }}
                            {% else -%}
                              {{ filter.range_max | money }}
                            {%- endif -%}
                          </span>
                          {% render 'icon-close' %}
                        </span>
                        <span class="visually-hidden">{{ 'products.facets.clear_filter' | t }}</span>
                      </a>
                    </facet-remove>
                  {%- endif -%}
                {% endif %}
              {%- endfor -%}
              <facet-remove>
                <a href="{{ search_url }}" class="active-facets__button-remove focus-inset body-small">
                  <span>{{ 'products.facets.clear_all' | t }}</span>
                </a>
              </facet-remove>
            </div>
          </div>
        </div>
      {%- endif -%}

      <div
        class="
          collection-wrapper collection-wrapper--{{ section.settings.columns }}
          collection-wrapper--{{ section.settings.filter_type }}
        "
      >
        {%- if search.filters != empty -%}
          {%- if section.settings.enable_filtering or section.settings.enable_sorting -%}
            {% render 'facets',
              results: search,
              enable_filtering: section.settings.enable_filtering,
              enable_sorting: section.settings.enable_sorting,
              filter_type: section.settings.filter_type,
              enable_default_open: section.settings.enable_default_open,
              image_filter_ratio: section.settings.image_filter_ratio,
              image_filter_fit: section.settings.image_filter_fit,
              image_filter_columns: section.settings.image_filter_columns,
              highlight_type: section.settings.highlight_type,
              total_selected_filters: section.settings.enable_total_selected
            %}
          {%- endif -%}
        {%- endif -%}
        {% if search.results.size > 0 %}
          {% liquid
            assign search_products = search.results | where: 'object_type', 'product'
            assign search_products_size = search_products | size
          %}
          {% if search_products_size > 0 %}
            <div class="search-results__top search-results__top--products collection-top collection-top__container container">
              <div class="collection-top__container-left">
                <div class="facets__product-count" role="status">
                  <h2 class="facets__product-count-text h6">
                    <span id="ProductCount">
                      {%- if search_products_size == 1 -%}
                        {{ 'templates.search.item_count.one' | t: count: search_products_size }}
                      {%- else -%}
                        {{ 'templates.search.item_count.other' | t: count: search_products_size }}
                      {%- endif -%}
                    </span>
                  </h2>
                </div>
                {%- if section.settings.enable_sorting -%}
                  <facet-filters-form class=" no-js-hidden">
                    <form id="FacetSortForm" class="facets-sorting">
                      <div class="facets-sorting__label">
                        {{- 'products.facets.sort_by_label' | t -}}
                      </div>
                      <div class="facet-filters__field facets-sorting__field">
                        {%- assign sort_by = search.sort_by | default: search.default_sort_by -%}

                        <div class="facet-sort-select">
                          <input
                            class="facet-sort-select__input"
                            type="hidden"
                            name="sort_by"
                            id="SortBy"
                            value="{{ sort_by }}"
                            aria-describedby="a11y-refresh-page-message"
                          >
                          <div class="facet-sort-select__select focus-inset" tabindex="0">
                            <span class="facet-sort-select__current">
                              {%- for option in search.sort_options -%}
                                {% if option.value == sort_by %}
                                  {{- option.name | escape -}}
                                {% endif %}
                              {%- endfor -%}
                            </span>
                            <div class="facet-sort-select__icon">
                              {% render 'icon-caret' %}
                            </div>
                          </div>
                          <div class="facet-sort-select__options">
                            {%- for option in search.sort_options -%}
                              <div
                                class="facet-sort-select__option focus-inset{% if option.value == sort_by %} facet-sort-select__option--selected{% endif %}"
                                data-value="{{ option.value | escape }}"
                                tabindex="0"
                              >
                                {{- option.name | escape -}}
                              </div>
                            {%- endfor -%}
                          </div>
                        </div>
                      </div>
                    </form>
                  </facet-filters-form>
                  <noscript>
                    <facet-filters-form class="small-hide">
                      <form id="FacetSortForm" class="facets-sorting">
                        <label class="facets-sorting__label" for="SortByNoScript">
                          {{- 'products.facets.sort_by_label' | t -}}
                        </label>
                        <div class="facet-filters__field facets-sorting__field">
                          {%- assign sort_by = search.sort_by | default: search.default_sort_by -%}
                          <div class="select field">
                            <select
                              name="sort_by"
                              class="select__select focus-inset"
                              id="SortByNoScript"
                              aria-describedby="a11y-refresh-page-message"
                            >
                              {%- for option in search.sort_options -%}
                                <option
                                  value="{{ option.value | escape }}"
                                  {% if option.value == sort_by %}
                                    selected="selected"
                                  {% endif %}
                                >
                                  {{ option.name | escape }}
                                </option>
                              {%- endfor -%}
                            </select>
                            {% render 'icon-caret' %}
                          </div>
                        </div>
                        <button type="submit" class="button button--tertiary focus-inset">
                          {{- 'products.facets.sort_button' | t -}}
                        </button>
                      </form>
                    </facet-filters-form>
                  </noscript>
                {%- endif -%}
              </div>

              {% if section.settings.enable_card_size_toggle or section.settings.enable_filtering %}
                <div class="collection-top__container-right">
                  {% if section.settings.enable_filtering %}
                    {% liquid
                      assign total_active_values = 0
                      assign has_active_facets = false
                      for filter in search.filters
                        if filter.active_values.size > 0 or filter.min_value.value != null or filter.max_value.value != null
                          assign has_active_facets = true
                          case filter.type
                            when 'boolean', 'list'
                              assign total_active_values = total_active_values | plus: filter.active_values.size
                            when 'price_range'
                              if filter.min_value.value != null or filter.max_value.value != null
                                assign total_active_values = total_active_values | plus: 1
                              endif
                          endcase
                        endif
                      endfor
                    %}
                    <button
                      type="button"
                      data-desktop-modal="FacetsWrapperDesktop-{{ section.id }}"
                      class="button button--simple facets-button-show {{ section.settings.filter_type }} no-js-hidden focus-inset"
                      aria-label="{{ 'products.facets.filters' | t }}"
                    >
                      <span class="show-label" aria-hidden="true">
                        {%- render 'icon-filter' -%}
                      </span>
                      <span class="hide-label hidden" aria-hidden="true">
                        {%- render 'icon-close' -%}
                      </span>
                      <span class="button__label" data-label="{{ 'products.facets.filters' | t }}">
                        {{ 'products.facets.filters' | t -}}
                      </span>
                      {% if has_active_facets and total_active_values > 0 %}
                        <span class="facets-button-show__counter" aria-hidden="true">
                          {{- total_active_values -}}
                        </span>
                      {% endif %}
                    </button>
                  {% endif %}
                  {% if section.settings.enable_card_size_toggle %}
                    <div class="collection-top__layout-toggler">
                      <span class="collection-top__layout-toggler-label subtitle body-small">
                        {{- 'products.facets.view' | t | append: ':' -}}
                      </span>
                      <div class="collection-top__layout-toggler-buttons">
                        {% liquid
                          if cart.attributes['catalog-card-size'] != null and cart.attributes['catalog-card-size'] != blank
                            assign catalog_card_size = cart.attributes['catalog-card-size']
                          else
                            assign catalog_card_size = section.settings.default_card_size
                          endif
                        %}
                        <button
                          type="button"
                          data-card-size="large"
                          aria-label="{{ 'products.facets.switch_to_large_cards' | t }}"
                          tabindex="0"
                          class="collection-top__layout-toggler-button js-switch-card-size focus-inset {% if catalog_card_size == 'large' %} active-layout{% endif %}"
                        >
                          <span class="visually-hidden">{{ 'products.facets.switch_to_large_cards' | t }}</span>

                          {% render 'icon-cells-large' %}
                        </button>
                        <button
                          type="button"
                          aria-label="{{ 'products.facets.switch_to_small_cards' | t }}"
                          data-card-size="small"
                          tabindex="0"
                          class="collection-top__layout-toggler-button js-switch-card-size focus-inset {% if catalog_card_size == 'small' %} active-layout{% endif %}"
                        >
                          <span class="visually-hidden">{{ 'products.facets.switch_to_small_cards' | t }}</span>
                          {% render 'icon-cells-small' %}
                        </button>
                      </div>
                      <div></div>
                    </div>
                  {% endif %}
                </div>
              {% endif %}
            </div>
          {% endif %}
        {% endif %}
        <div
          id="ProductGridContainer"
          {% if section.settings.enable_card_size_toggle %}
            data-catalog-card-size="{% if cart.attributes['catalog-card-size'] %}{{ cart.attributes['catalog-card-size'] | escape }}{% else %}{{ section.settings.default_card_size }}{% endif %}"
          {% endif %}
          class="products-grid {% if section.settings.enable_filtering == false or filter_show == false %} products-grid_no-filter {%- endif -%}"
        >
          {% paginate search.results by 24 %}
            {%- if search.results.size == 0 and search.filters != empty -%}
              <div
                class="template-search__results collection collection-product collection--empty"
                id="product-grid"
                data-id="{{ section.id }}"
              >
                <div class="loading-overlay">
                  <div class="loading-overlay__spinner">
                    {% render 'icon-loading' %}
                  </div>
                </div>
                <div class="title-wrapper">
                  <h2 class="h6 richtext-link-inside">
                    {{ 'sections.collection_template.empty_results' | t }}
                    <br>
                    {{ 'sections.collection_template.results_use_fewer_filters_html' | t: link: search_url }}
                  </h2>
                </div>
              </div>
            {%- else -%}
              <div
                class="template-search__results collection collection-product"
                id="product-grid"
                data-id="{{ section.id }}"
              >
                <div class="loading-overlay">
                  <div class="loading-overlay__spinner">
                    {% render 'icon-loading' %}
                  </div>
                </div>

                {% liquid
                  assign search_products = search.results | where: 'object_type', 'product'
                  assign search_products_size = search_products | size
                  assign search_articles = search.results | where: 'object_type', 'article'
                  assign search_articles_size = search_articles | size
                  assign search_pages = search.results | where: 'object_type', 'page'
                  assign search_pages_size = search_pages | size
                %}

                {% if search_products_size != 0 %}
                  <div class="grid-container">
                    <div
                      class="
                        collection-product-list {% if section.settings.enable_card_size_toggle == false %} collection-product-list--{{ section.settings.columns }} collection-product-list-mobile--{{section.settings.columns_mobile}}{% endif %}
                        {% if section.settings.enable_filtering %} collection-results_filters {% endif %}
                      "
                      id="Grid-Of-Products"
                      role="list"
                    >
                      {%- for item in search_products -%}
                        <div class="template-search__item collection-product-card">
                          {% render 'product-card',
                            product_card_product: item,
                            columns: 4,
                            section_obj: section,
                            section_id: section.id,
                            forloop_index: forloop.index,
                            information_block_type: section.settings.information_block_type,
                            show_quick_add_overwrite: section.settings.show_quick_add,
                            quick_add_block_type: section.settings.quick_add_block_type,
                            show_quick_view_overwrite: section.settings.show_quick_view,
                            dynamic_collection_grid: section.settings.enable_card_size_toggle
                          %}
                        </div>
                      {%- endfor -%}
                    </div>
                  </div>
                {% endif %}

                {% if search_articles_size != 0 %}
                  <div class="search-results__top container">
                    <h2 class="search-results__title h6">
                      {{- 'templates.search.article_count' | t: count: search_articles_size -}}
                    </h2>
                  </div>
                  <div class="grid-container">
                    <div
                      class="
                        collection-results collection-results--articles
                        collection-product-list--4
                        {% if section.settings.enable_filtering == false or filter_show == false %} collection-results_no-filter {%- endif -%}
                      "
                      role="list"
                    >
                      {%- for item in search_articles -%}
                        {%- case item.object_type -%}
                          {%- when 'article' -%}
                            <div class="template-search__item article article--card">
                              {% render 'article-card',
                                article: item,
                                article_heading_font_size: section.settings.article_heading_font_size,
                                article_heading_tag: section.settings.article_heading_tag,
                                object_fit: section.settings.object_fit,
                                aspect_ratio: section.settings.article_image_ratio,
                                show_image: section.settings.show_image,
                                show_date: section.settings.show_date,
                                show_author: section.settings.show_author,
                                show_tags: section.settings.show_tags,
                                show_excerpt: section.settings.show_excerpt
                              %}
                            </div>
                        {%- endcase -%}
                      {%- endfor -%}
                    </div>
                  </div>
                {% endif %}

                {% if search_pages_size != 0 %}
                  <div class="search-results__top container">
                    <h2 class="search-results__title h6">
                      {{ 'templates.search.page_count' | t: count: search_pages_size }}
                    </h2>
                  </div>
                  <div class="grid-container">
                    <div
                      class="
                        collection-results collection-results--pages
                        collection-product-list--6
                        {% if section.settings.enable_filtering == false or filter_show == false %} collection-results_no-filter {%- endif -%}
                      "
                      role="list"
                    >
                      {%- for item in search_pages -%}
                        {%- case item.object_type -%}
                          {%- when 'page' -%}
                            <div class="template-search__item page--card">
                              {% render 'page-card', url: item.url, title: item.title %}
                            </div>
                        {%- endcase -%}
                      {%- endfor -%}
                    </div>
                  </div>
                {% endif %}

                {%- if paginate.pages > 1 -%}
                  {% render 'pagination', paginate: paginate %}
                {%- endif -%}
              </div>
            {%- endif -%}
          {% endpaginate %}
        </div>
      </div>
    </div>
  {%- endif -%}
</div>

<script>
  (() => {
    function initSearch() {
      const input = document.querySelector('#Search-In-Template');

      if (input.closest('predictive-search')) return;
      if (!input) return;

      const form = input.form;
      const resetBtn = form?.querySelector('.search__button--reset');

      input.focus();
      const length = input.value.length;
      input.setSelectionRange(length, length);

      input?.addEventListener('input', (event) => {
        if (event.target.value) {
          resetBtn.style.display = 'flex';
        } else {
          resetBtn.style.display = 'none';
        }
      });

      resetBtn?.addEventListener('click', (event) => {
        event.preventDefault();

        if (input) {
          input.value = '';
          input.focus();
        }

        resetBtn.style.display = 'none';
      });
    }
    window.addEventListener('load', initSearch);
  })();
</script>

{% schema %}
{
  "name": "t:sections.main-search.name",
  "tag": "section",
  "class": "section-search",
  "settings": [
    {
      "type": "checkbox",
      "id": "predictive_search_enabled",
      "default": true,
      "label": "t:sections.main-search.settings.predictive_search_enabled.label"
    },

    {
      "type": "header",
      "content": "t:sections.main-search.settings.header__trending.content"
    },
    {
      "type": "checkbox",
      "id": "enable_trending_search",
      "default": true,
      "label": "t:sections.main-search.settings.enable_trending_search.label"
    },
    {
      "type": "text",
      "id": "trending_search_title",
      "default": "t:sections.main-search.settings.trending_search_title.default",
      "label": "t:sections.main-search.settings.trending_search_title.label",
      "visible_if": "{{ section.settings.enable_trending_search == true }}"
    },
    {
      "type": "link_list",
      "id": "trending_search_menu",
      "label": "t:sections.main-search.settings.trending_search_menu.label",
      "visible_if": "{{ section.settings.enable_trending_search == true }}"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection.settings.per_row_options_header.content"
    },
    {
      "type": "checkbox",
      "id": "enable_card_size_toggle",
      "default": true,
      "label": "t:sections.main-collection.settings.enable_card_size_toggle.label"
    },
    {
      "type": "select",
      "id": "default_card_size",
      "label": "t:sections.main-collection.settings.default_card_size.label",
      "default": "large",
      "options": [
        {
          "value": "small",
          "label": "t:sections.main-collection.settings.default_card_size.options__1.label"
        },
        {
          "value": "large",
          "label": "t:sections.main-collection.settings.default_card_size.options__2.label"
        }
      ],
      "info": "t:sections.main-collection.settings.default_card_size.info",
      "visible_if": "{{ section.settings.enable_card_size_toggle == true }}"
    },
    {
      "type": "range",
      "id": "columns",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "t:sections.main-collection.settings.columns.label",
      "visible_if": "{{ section.settings.enable_card_size_toggle == false }}"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "1",
      "label": "t:sections.main-collection.settings.columns_mobile.label",
      "options": [
        {
          "value": "1",
          "label": "t:sections.main-collection.settings.columns_mobile.options__1.label"
        },
        {
          "value": "2",
          "label": "t:sections.main-collection.settings.columns_mobile.options__2.label"
        }
      ],
      "visible_if": "{{ section.settings.enable_card_size_toggle == false }}"
    },
    {
      "type": "header",
      "content": "t:sections.all.card.header_product_card.content"
    },
    {
      "type": "select",
      "id": "information_block_type",
      "label": "t:sections.all.card.information_block_type.label",
      "info": "t:sections.all.card.information_block_type.info",
      "options": [
        {
          "value": "static",
          "label": "t:sections.all.card.information_block_type.options__1.label"
        },
        {
          "value": "reveal",
          "label": "t:sections.all.card.information_block_type.options__2.label"
        }
      ],
      "default": "static",
      "visible_if": "{{ settings.product_card_type == 'overlay' and section.settings.enable_card_size_toggle == false }}"
    },
    {
      "type": "checkbox",
      "id": "show_quick_add",
      "default": true,
      "label": "t:sections.all.card.show_quick_add.label",
      "info": "t:sections.all.card.show_quick_add.info",
      "visible_if": "{{ settings.quickadd_show == true or settings.show_quick_options != blank }}"
    },
    {
      "type": "select",
      "id": "quick_add_block_type",
      "label": "t:sections.all.card.quick_add_block_type.label",
      "info": "t:sections.all.card.quick_add_block_type.info",
      "options": [
        {
          "value": "static",
          "label": "t:sections.all.card.quick_add_block_type.options__1.label"
        },
        {
          "value": "reveal",
          "label": "t:sections.all.card.quick_add_block_type.options__2.label"
        }
      ],
      "default": "reveal",
      "visible_if": "{{ settings.product_card_type == 'overlay' and section.settings.enable_card_size_toggle == false and section.settings.show_quick_add == true and section.settings.information_block_type == 'static' }}"
    },
    {
      "type": "checkbox",
      "id": "show_quick_view",
      "default": true,
      "label": "t:sections.all.card.show_quick_view.label",
      "info": "t:sections.all.card.show_quick_view.info",
      "visible_if": "{{ settings.quickview_show == true }}"
    },
    {
      "type": "header",
      "content": "t:sections.main-search.settings.header__2.content"
    },
    {
      "type": "select",
      "id": "article_heading_font_size",
      "options": [
        {
          "value": "h6",
          "label": "t:sections.all.heading_size.options__6.label"
        },
        {
          "value": "h5",
          "label": "t:sections.all.heading_size.options__5.label"
        },
        {
          "value": "h4",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "h3",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__2.label"
        }
      ],
      "default": "h5",
      "label": "t:sections.blog-posts.settings.article_heading_font_size.label"
    },
    {
      "type": "select",
      "id": "article_heading_tag",
      "options": [
        {
          "value": "h1",
          "label": "t:sections.all.heading_tag.options__1.label"
        },
        {
          "value": "h2",
          "label": "t:sections.all.heading_tag.options__2.label"
        },
        {
          "value": "h3",
          "label": "t:sections.all.heading_tag.options__3.label"
        },
        {
          "value": "h4",
          "label": "t:sections.all.heading_tag.options__4.label"
        },
        {
          "value": "h5",
          "label": "t:sections.all.heading_tag.options__5.label"
        },
        {
          "value": "h6",
          "label": "t:sections.all.heading_tag.options__6.label"
        },
        {
          "value": "div",
          "label": "t:sections.all.heading_tag.options__7.label"
        }
      ],
      "default": "h3",
      "label": "t:sections.blog-posts.settings.article_heading_tag.label",
      "info": "t:sections.all.heading_tag.info"
    },
    {
      "type": "checkbox",
      "id": "show_image",
      "default": true,
      "label": "t:sections.main-blog.settings.show_image.label"
    },
    {
      "type": "select",
      "id": "article_image_ratio",
      "options": [
        {
          "value": "landscape",
          "label": "t:sections.all.card.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.all.card.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.all.card.image_ratio.options__3.label"
        }
      ],
      "default": "portrait",
      "label": "t:sections.all.card.image_ratio.label",
      "visible_if": "{{ section.settings.show_image }}"
    },
    {
      "type": "select",
      "id": "object_fit",
      "options": [
        {
          "value": "contain",
          "label": "t:sections.all.card.image_fit.options__1.label"
        },
        {
          "value": "cover",
          "label": "t:sections.all.card.image_fit.options__2.label"
        }
      ],
      "default": "cover",
      "label": "t:sections.all.card.image_fit.label",
      "visible_if": "{{ section.settings.show_image }}"
    },
    {
      "type": "checkbox",
      "id": "show_date",
      "default": true,
      "label": "t:sections.main-blog.settings.show_date.label"
    },
    {
      "type": "checkbox",
      "id": "show_tags",
      "default": true,
      "label": "t:sections.main-blog.settings.show_tags.label"
    },
    {
      "type": "checkbox",
      "id": "show_author",
      "default": true,
      "label": "t:sections.main-blog.settings.show_author.label"
    },
    {
      "type": "checkbox",
      "id": "show_excerpt",
      "default": true,
      "label": "t:sections.main-blog.settings.show_excerpt.label"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection.settings.header__1.content"
    },
    {
      "type": "checkbox",
      "id": "enable_filtering",
      "default": false,
      "label": "t:sections.main-collection.settings.enable_filtering.label",
      "info": "t:sections.main-collection.settings.enable_filtering.info"
    },
    {
      "type": "select",
      "id": "filter_type",
      "options": [
        {
          "value": "vertical",
          "label": "t:sections.main-collection.settings.filter_type.options__1.label"
        },
        {
          "value": "horizontal",
          "label": "t:sections.main-collection.settings.filter_type.options__2.label"
        }
      ],
      "default": "vertical",
      "label": "t:sections.main-collection.settings.filter_type.label",
      "info": "t:sections.main-collection.settings.filter_type.info",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "select",
      "id": "highlight_type",
      "options": [
        {
          "value": "border",
          "label": "t:sections.main-collection.settings.highlight_type.options__1.label"
        },
        {
          "value": "color",
          "label": "t:sections.main-collection.settings.highlight_type.options__2.label"
        }
      ],
      "default": "border",
      "label": "t:sections.main-collection.settings.highlight_type.label",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "checkbox",
      "id": "enable_total_selected",
      "default": false,
      "label": "t:sections.main-collection.settings.enable_total_selected.label",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "checkbox",
      "id": "enable_default_open",
      "default": true,
      "label": "t:sections.main-collection.settings.enable_default_open.label",
      "visible_if": "{{ section.settings.enable_filtering == true and section.settings.filter_type == 'vertical' }}"
    },
    {
      "type": "checkbox",
      "id": "enable_sorting",
      "default": false,
      "label": "t:sections.main-collection.settings.enable_sorting.label"
    },
    {
      "type": "header",
      "content": "t:sections.main-collection.settings.image_filter.content",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "select",
      "id": "image_filter_ratio",
      "options": [
        {
          "value": "3/2",
          "label": "t:sections.all.card.image_ratio.options__1.label"
        },
        {
          "value": "3/4",
          "label": "t:sections.all.card.image_ratio.options__2.label"
        },
        {
          "value": "1/1",
          "label": "t:sections.all.card.image_ratio.options__3.label"
        }
      ],
      "default": "3/4",
      "label": "t:sections.all.card.image_ratio.label",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "select",
      "id": "image_filter_fit",
      "options": [
        {
          "value": "contain",
          "label": "t:sections.all.card.image_fit.options__1.label"
        },
        {
          "value": "cover",
          "label": "t:sections.all.card.image_fit.options__2.label"
        }
      ],
      "default": "cover",
      "label": "t:sections.all.card.image_fit.label",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "range",
      "id": "image_filter_columns",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3,
      "label": "t:sections.main-collection.settings.image_filter_columns.label",
      "visible_if": "{{ section.settings.enable_filtering == true }}"
    },
    {
      "type": "header",
      "content": "t:sections.all.section-margin.header.content"
    },
    {
      "type": "select",
      "id": "margin_top",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "no-indent",
      "label": "t:sections.all.section-margin.margin_top.label"
    },
    {
      "type": "select",
      "id": "margin_bottom",
      "options": [
        {
          "value": "no-indent",
          "label": "t:sections.all.section-margin.options__1.label"
        },
        {
          "value": "xs",
          "label": "t:sections.all.section-margin.options__2.label"
        },
        {
          "value": "s",
          "label": "t:sections.all.section-margin.options__3.label"
        },
        {
          "value": "m",
          "label": "t:sections.all.section-margin.options__4.label"
        },
        {
          "value": "l",
          "label": "t:sections.all.section-margin.options__5.label"
        },
        {
          "value": "xl",
          "label": "t:sections.all.section-margin.options__6.label"
        }
      ],
      "default": "no-indent",
      "label": "t:sections.all.section-margin.margin_bottom.label"
    }
  ]
}
{% endschema %}
