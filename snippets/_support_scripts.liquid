<script>
  window.routes = {
    cart_add_url: '{{ routes.cart_add_url }}',
    cart_change_url: '{{ routes.cart_change_url }}',
    cart_update_url: '{{ routes.cart_update_url }}',
    cart_url: '{{ routes.cart_url }}',
    predictive_search_url: '{{ routes.predictive_search_url }}'
  };

  window.cartStrings = {
    error: `{{ 'general.cart.cart_error' | t }}`,
    quantityError: `{{ 'general.cart.cart_quantity_error_html' | t }}`
  }

  window.variantStrings = {
    addToCart: `{%- if settings.add_to_cart_label != blank -%}{{ settings.add_to_cart_label }}{%- else -%}{{ 'products.product.add_to_cart' | t }}{%- endif -%}`,
    soldOut: `{{ 'products.product.sold_out' | t }}`,
    unavailable: `{{ 'products.product.unavailable' | t }}`,
    unavailable_with_option: `{{ 'products.product.value_unavailable' | t: option_value: '[value]' }}`
  }

  window.theme = {
    moneyFormat: {{ shop.money_format | json }},
    quickviewMore: `{{ 'products.product.view_details' | t }}`,
  }

  const calcVh = () => {
    const vh = window.innerHeight * 0.01;
    document.documentElement.style.setProperty("--vh", `${vh}px`);
  }
  window.addEventListener("load", calcVh);
  window.addEventListener("resize", calcVh);
</script>

{%- if request.design_mode -%}
  <script>
    if (window.Shopify && window.Shopify.theme && navigator && navigator.sendBeacon && window.Shopify.designMode) {
      const themeKey = `dataSent_{{ theme.name }}`;
      if (!localStorage.getItem(themeKey)) {
        navigator.sendBeacon(
          'https://cdn.ashstonestudios.com/shopify-beacon/sent/',
          new URLSearchParams({
            slug: window.Shopify.theme.schema_name,
            shop: window.Shopify.shop,
            role: window.Shopify.theme.role,
            route: window.location.pathname,
            themeId: window.Shopify.theme.id,
            themeStoreId: window.Shopify.theme.theme_store_id || 0,
            isThemeEditor: !!window.Shopify.designMode,
            themeName: '{{ theme.name }}',
            shopName: '{{ shop.name }}',
            shopEmail: '{{ shop.email }}',
          })
        );
        localStorage.setItem(themeKey, 'true');
      }
    }
  </script>
{%- endif -%}
